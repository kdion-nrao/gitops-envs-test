name: Actions Runner Controller Demo
on:
  workflow_dispatch:

jobs:
  Use-ARC:
    runs-on: aws-arc-runner-local
    steps:
      - run: echo "🎉 This job uses runner scale set runners!"
      - run: echo "Repo: ${{ github.repository }}, Ref: ${{ github.ref }}"
