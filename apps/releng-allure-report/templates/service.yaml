apiVersion: v1
kind: Service
metadata:
  name: {{ include "releng-allure-report.fullname" . }}
  labels:
    {{- include "releng-allure-report.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "releng-allure-report.selectorLabels" . | nindent 4 }}
