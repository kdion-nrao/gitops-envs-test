replicaCount: 1

storage:
  remote:
    backend: s3
    bucket: allure-reports
    region: auto
    forcePathStyle: false

image:
  repository: "nrao.repo.sonatype.app/cicdpilot/allure/allure-report-storage"
  tag: "v1.0.1-patched"

imageCredentials:
  registry: nrao.repo.sonatype.app
