color: blue

contextPath: /dev

image:
  pullPolicy: Always
  tag: edge

ingress:
  tls:
    - hosts:
        - dummy.localhost
  hosts:
    - host: dummy.localhost
      paths:
        - path: /dev
          pathType: Prefix
